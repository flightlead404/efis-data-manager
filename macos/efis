#!/bin/bash
"""
EFIS Data Manager CLI wrapper for macOS.
"""

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute the Python CLI script
python3 "$SCRIPT_DIR/efis_cli.py" "$@"