# Windows Service Dependencies - Flexible Versions
# Use this if the main requirements.txt fails due to version conflicts

pywin32>=306,<400
pyyaml>=6.0.0,<7.0.0
requests>=2.28.0,<3.0.0
psutil>=5.8.0,<6.0.0
watchdog>=2.0.0,<4.0.0
colorlog>=6.0.0,<7.0.0

# Alternative packages if specific versions fail
# Uncomment these if needed:
# pywin32  # Latest available version
# pyyaml   # Latest available version
# requests # Latest available version
# psutil   # Latest available version
# watchdog # Latest available version
# colorlog # Latest available version